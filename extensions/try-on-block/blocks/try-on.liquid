{% if product %}
<div
  id="vto"
  data-pid="{{ product.id }}"
  data-ptitle="{{ product.title | escape }}"
  data-pimg="{{ product.featured_image | image_url: width: 1024 }}"
  data-shop="{{ shop.permanent_domain }}"
  data-category="{{ block.settings.garment_category }}"
>
  <button id="vto-btn" class="vto-btn" type="button">
    <span class="vto-btn__shimmer"></span>
    <span class="vto-btn__label">{{ block.settings.button_text }}</span>
    <span class="vto-btn__tag">AI</span>
  </button>

  <div id="vto-popup" class="vto-popup" style="display:none;">
    <div class="vto-popup__inner">
      <img src="{{ product.featured_image | image_url: width: 80 }}" class="vto-popup__thumb" width="44" height="44" alt="" />
      <div class="vto-popup__text">
        <strong>See it on you ✨</strong>
        <span>{{ block.settings.popup_subtitle }}</span>
      </div>
      <button id="vto-popup-cta" class="vto-popup__cta" type="button">Try</button>
      <button id="vto-popup-close" class="vto-popup__x" type="button" aria-label="Close">✕</button>
    </div>
  </div>

  <div id="vto-overlay" class="vto-overlay" style="display:none;">
    <div class="vto-modal" role="dialog" aria-modal="true">
      <button id="vto-modal-close" class="vto-close" type="button" aria-label="Close">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
      </button>

      <!-- ════ UPLOAD STATE ════ -->
      <div id="vto-s-upload" class="vto-state">
        <div class="vto-modal__top">
          <div class="vto-badge">✨ Virtual Try-On</div>
          <h2 class="vto-title">{{ block.settings.modal_title }}</h2>
          <p class="vto-sub">{{ block.settings.modal_subtitle }}</p>
        </div>

        <!-- Split View: Product + User Photo -->
        <div class="vto-split">
          <div class="vto-split__product">
            <div class="vto-split__label">The Product</div>
            <div class="vto-split__img-wrap">
              <img src="{{ product.featured_image | image_url: width: 600 }}" class="vto-split__img" alt="{{ product.title | escape }}" />
            </div>
            <div class="vto-split__name">{{ product.title }}</div>
            <div class="vto-split__price">{{ product.price | money }}</div>
          </div>

          <div class="vto-split__divider">
            <div class="vto-split__plus">+</div>
          </div>

          <div class="vto-split__user">
            <div class="vto-split__label">You</div>
            <div id="vto-user-zone" class="vto-split__img-wrap vto-split__drop">
              <input type="file" id="vto-file" accept="image/jpeg,image/png,image/webp" />
              <div id="vto-dz-empty" class="vto-split__empty">
                <div class="vto-split__upload-icon">
                  <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                    <polyline points="17 8 12 3 7 8"/>
                    <line x1="12" y1="3" x2="12" y2="15"/>
                  </svg>
                </div>
                <p class="vto-split__upload-text">Upload your photo</p>
                <p class="vto-split__upload-hint">JPG, PNG or WebP</p>
              </div>
              <div id="vto-dz-preview" style="display:none;">
                <img id="vto-preview-img" class="vto-split__img" alt="Your photo" />
                <button id="vto-change-photo" class="vto-split__change" type="button">Change</button>
              </div>
            </div>
          </div>
        </div>

        <button id="vto-submit" class="vto-main-cta" type="button" disabled>
          <span id="vto-submit-text">Upload photo to try on</span>
        </button>
        <p class="vto-privacy">🔒 Your photo is never stored</p>
      </div>

      <!-- ════ MERGE ANIMATION STATE ════ -->
      <div id="vto-s-merge" class="vto-state" style="display:none;">
        <div class="vto-merge-wrap">
          <div class="vto-merge-title">Creating your look...</div>
          <div class="vto-merge-canvas">
            <img id="vto-merge-product" class="vto-merge__img vto-merge__left" src="{{ product.featured_image | image_url: width: 400 }}" alt="" />
            <div class="vto-merge__glow"></div>
            <img id="vto-merge-user" class="vto-merge__img vto-merge__right" alt="" />
          </div>
        </div>
      </div>

      <!-- ════ LOADING STATE ════ -->
      <div id="vto-s-loading" class="vto-state" style="display:none;">
        <div class="vto-loading-wrap">
          <div class="vto-loader-circle">
            <svg width="72" height="72" viewBox="0 0 72 72">
              <circle cx="36" cy="36" r="32" fill="none" stroke="#f0f4f8" stroke-width="2"/>
              <circle cx="36" cy="36" r="32" fill="none" stroke="#7c3aed" stroke-width="2.5" stroke-linecap="round" stroke-dasharray="160 200" class="vto-spinner-arc"/>
            </svg>
          </div>
          <h3 class="vto-loading__title" id="vto-loading-title">Analyzing your photo</h3>
          <p class="vto-loading__sub" id="vto-loading-sub">Detecting body shape & pose</p>
          <div class="vto-progress"><div class="vto-progress__bar"></div></div>
          <div class="vto-loading-steps">
            <div class="vto-lstep active" id="vto-ls1"><span class="vto-lstep__check"></span>Detecting body pose</div>
            <div class="vto-lstep" id="vto-ls2"><span class="vto-lstep__check"></span>Mapping garment fit</div>
            <div class="vto-lstep" id="vto-ls3"><span class="vto-lstep__check"></span>Rendering final look</div>
          </div>
          <p class="vto-loading__time">Usually takes 15–30 seconds</p>
        </div>
      </div>

      <!-- ════ RESULT STATE ════ -->
      <div id="vto-s-result" class="vto-state" style="display:none;">
        <div class="vto-result-wrap">
          <div class="vto-badge vto-badge--gold">🎉 Your Look is Ready</div>
          <div class="vto-result__img-wrap">
            <img id="vto-result-img" alt="Try-on result" />
            <div class="vto-result__shine"></div>
          </div>
          <div class="vto-result__product-tag">
            <img src="{{ product.featured_image | image_url: width: 80 }}" width="32" height="32" alt="" />
            <span>{{ product.title }}</span>
          </div>
          <div class="vto-result__btns">
            <button id="vto-retry" class="vto-btn-ghost" type="button">↻ Try Another</button>
            <button id="vto-addtocart" class="vto-main-cta vto-main-cta--sm" type="button">Add to Cart 🛒</button>
          </div>
          <p class="vto-disclaimer">Colors and fit shown in AI-generated previews may vary slightly from the actual product.</p>
          <div class="vto-powered">Powered by <a href="https://futuretechiez.in" target="_blank" rel="noopener noreferrer">Futuretechiez.in</a></div>
        </div>
      </div>

      <!-- ════ ERROR STATE ════ -->
      <div id="vto-s-error" class="vto-state" style="display:none;">
        <div class="vto-error-wrap">
          <div class="vto-error__emoji">😔</div>
          <h3 class="vto-error__title">Oops, something broke</h3>
          <p id="vto-error-msg" class="vto-error__msg"></p>
          <button id="vto-error-retry" class="vto-main-cta" type="button">Try Again</button>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
:root {
  --vto-primary: {{ block.settings.button_color }};
  --vto-primary-text: {{ block.settings.button_text_color }};
  --vto-r: {{ block.settings.button_radius }}px;
  --vto-ff: 'Inter', system-ui, -apple-system, sans-serif;
  --vto-accent: #7c3aed;
}
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

/* ── BUTTON ── */
.vto-btn {
  position: relative; display: flex; align-items: center; justify-content: center; gap: 10px;
  width: 100%; padding: 17px 24px; margin-top: 12px;
  background: #1a2b3c; color: #fff; border: 1.5px solid #2a4a5e;
  border-radius: var(--vto-r); font: 600 14px/1 var(--vto-ff);
  cursor: pointer; overflow: hidden; transition: all .25s ease; letter-spacing: 0.4px;
}
.vto-btn::before {
  content: ''; position: absolute; top: 0; left: -100%; width: 60%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(148,184,208,.12), transparent);
  animation: vto-btnShimmer 3s ease infinite;
}
@keyframes vto-btnShimmer { 0%{left:-100%} 100%{left:200%} }
.vto-btn:hover { background: #243d52; border-color: #94b8d0; transform: translateY(-1px); box-shadow: 0 6px 20px rgba(26,43,60,.25); }
.vto-btn:active { transform: translateY(0); }
.vto-btn__shimmer { display: none; }
.vto-btn__label { position: relative; z-index: 1; }
.vto-btn__tag { position: relative; z-index: 1; padding: 4px 10px; background: #94b8d0; border-radius: 6px; font-size: 10px; font-weight: 700; letter-spacing: 0.8px; color: #fff; }

/* ── POPUP ── */
.vto-popup { margin-top: 10px; animation: vto-slideUp .4s ease; }
.vto-popup__inner { display: flex; align-items: center; gap: 10px; padding: 12px 14px; background: #111; border-radius: 14px; position: relative; }
.vto-popup__thumb { width: 44px; height: 44px; border-radius: 10px; object-fit: cover; }
.vto-popup__text { flex: 1; display: flex; flex-direction: column; gap: 1px; }
.vto-popup__text strong { color: #fff; font: 600 13px/1.3 var(--vto-ff); }
.vto-popup__text span { color: #888; font: 400 11px/1.3 var(--vto-ff); }
.vto-popup__cta { padding: 7px 16px; background: #fff; color: #111; border: none; border-radius: 8px; font: 700 12px/1 var(--vto-ff); cursor: pointer; }
.vto-popup__x { position: absolute; top: -6px; right: -4px; background: #333; border: none; border-radius: 50%; width: 20px; height: 20px; color: #aaa; font-size: 10px; cursor: pointer; display: flex; align-items: center; justify-content: center; }

/* ── OVERLAY ── */
.vto-overlay { position: fixed; inset: 0; z-index: 999999; background: rgba(0,0,0,.65); backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px); display: flex; align-items: center; justify-content: center; padding: 16px; animation: vto-fadeIn .25s; }

/* ── MODAL ── */
.vto-modal { background: #fff; border-radius: 28px; max-width: 520px; width: 100%; max-height: 94vh; overflow-y: auto; padding: 32px 28px; position: relative; box-shadow: 0 32px 80px rgba(0,0,0,.3), 0 0 0 1px rgba(0,0,0,.04); animation: vto-scaleIn .3s cubic-bezier(.16,1,.3,1); }
.vto-close { position: absolute; top: 16px; right: 16px; width: 34px; height: 34px; background: #f5f5f5; border: none; border-radius: 50%; display: flex; align-items: center; justify-content: center; cursor: pointer; color: #555; z-index: 2; transition: background .15s; }
.vto-close:hover { background: #eee; }
.vto-badge { display: inline-block; padding: 6px 16px; background: linear-gradient(135deg, #f5f3ff, #fdf2f8); border: 1px solid rgba(139,92,246,.08); border-radius: 24px; font: 600 11px/1 var(--vto-ff); color: var(--vto-accent); margin-bottom: 14px; letter-spacing: 0.3px; }
.vto-badge--gold { background: linear-gradient(135deg, #fef9c3, #fde68a); border-color: rgba(234,179,8,.15); color: #92400e; }
.vto-modal__top { text-align: center; margin-bottom: 22px; }
.vto-title { margin: 0; font: 800 26px/1.15 var(--vto-ff); color: #111; letter-spacing: -0.8px; }
.vto-sub { margin: 8px 0 0; font: 400 14px/1.5 var(--vto-ff); color: #999; }

/* ── SPLIT VIEW ── */
.vto-split { display: flex; gap: 0; align-items: stretch; margin-bottom: 20px; }
.vto-split__product, .vto-split__user { flex: 1; text-align: center; }
.vto-split__label { font: 700 10px/1 var(--vto-ff); text-transform: uppercase; letter-spacing: 1.5px; color: #bbb; margin-bottom: 10px; }
.vto-split__img-wrap { width: 100%; aspect-ratio: 3/4; border-radius: 16px; overflow: hidden; background: #f8f8f8; border: 2px solid #f0f0f0; display: flex; align-items: center; justify-content: center; position: relative; transition: all .25s; }
.vto-split__img { width: 100%; height: 100%; object-fit: cover; }
.vto-split__name { font: 600 13px/1.3 var(--vto-ff); color: #111; margin-top: 8px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; padding: 0 4px; }
.vto-split__price { font: 600 12px/1 var(--vto-ff); color: var(--vto-accent); margin-top: 3px; }
.vto-split__divider { display: flex; align-items: center; justify-content: center; width: 44px; flex-shrink: 0; }
.vto-split__plus { width: 32px; height: 32px; border-radius: 50%; background: linear-gradient(135deg, #7c3aed, #a855f7); color: #fff; font: 800 18px/32px var(--vto-ff); text-align: center; box-shadow: 0 4px 16px rgba(124,58,237,.25); }
.vto-split__drop { cursor: pointer; border: 2px dashed #d4d4d4; background: #fafafa; }
.vto-split__drop:hover { border-color: var(--vto-accent); background: #faf8ff; }
.vto-split__drop input[type="file"] { position: absolute; inset: 0; opacity: 0; cursor: pointer; z-index: 2; }
.vto-split__empty { padding: 20px 8px; }
.vto-split__upload-icon { width: 52px; height: 52px; margin: 0 auto 12px; background: linear-gradient(135deg, #f5f3ff, #fdf2f8); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: var(--vto-accent); }
.vto-split__upload-text { font: 600 13px/1 var(--vto-ff); color: #333; margin: 0; }
.vto-split__upload-hint { font: 400 11px/1 var(--vto-ff); color: #bbb; margin: 6px 0 0; }
.vto-split__change { position: absolute; bottom: 8px; left: 50%; transform: translateX(-50%); background: rgba(0,0,0,.6); color: #fff; border: none; border-radius: 8px; padding: 5px 14px; font: 600 11px/1 var(--vto-ff); cursor: pointer; z-index: 3; }
#vto-dz-preview { width: 100%; height: 100%; }
#vto-dz-preview .vto-split__img { position: absolute; inset: 0; }

/* ── MERGE ANIMATION ── */
.vto-merge-wrap { text-align: center; padding: 20px 0; }
.vto-merge-title { font: 700 20px/1 var(--vto-ff); color: #111; margin-bottom: 24px; animation: vto-pulse 1.5s ease infinite; }
.vto-merge-canvas { position: relative; height: 320px; display: flex; align-items: center; justify-content: center; overflow: hidden; border-radius: 20px; background: linear-gradient(135deg, #f8f8ff, #fdf2f8); }
.vto-merge__img { width: 160px; height: 240px; object-fit: cover; border-radius: 16px; position: absolute; transition: all 1.2s cubic-bezier(.16,1,.3,1); box-shadow: 0 12px 40px rgba(0,0,0,.15); }
.vto-merge__left { left: 20%; transform: rotate(-3deg); }
.vto-merge__right { right: 20%; transform: rotate(3deg); }
.vto-merge__left.merging { left: 50%; transform: translateX(-50%) rotate(0deg) scale(1.1); opacity: .5; }
.vto-merge__right.merging { right: 50%; transform: translateX(50%) rotate(0deg) scale(1.1); opacity: .5; }
.vto-merge__glow { position: absolute; width: 120px; height: 120px; border-radius: 50%; background: radial-gradient(circle, rgba(124,58,237,.3), transparent 70%); opacity: 0; transition: opacity .8s; }
.vto-merge__glow.active { opacity: 1; animation: vto-glowPulse 1.5s ease infinite; }
@keyframes vto-glowPulse { 0%,100%{transform:scale(1);opacity:.6} 50%{transform:scale(1.6);opacity:1} }

/* ── CTA ── */
.vto-main-cta { width: 100%; padding: 16px; background: var(--vto-primary); color: var(--vto-primary-text); border: none; border-radius: var(--vto-r); font: 700 15px/1 var(--vto-ff); cursor: pointer; transition: all .2s cubic-bezier(.16,1,.3,1); position: relative; overflow: hidden; letter-spacing: 0.2px; }
.vto-main-cta:disabled { opacity: .3; cursor: default; }
.vto-main-cta:not(:disabled):hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(0,0,0,.15); }
.vto-main-cta:not(:disabled):active { transform: translateY(0); }
.vto-main-cta--sm { flex: 1.4; }
.vto-btn-ghost { flex: 1; padding: 14px; background: #f5f5f5; color: #333; border: none; border-radius: var(--vto-r); font: 600 14px/1 var(--vto-ff); cursor: pointer; transition: background .15s; }
.vto-btn-ghost:hover { background: #eee; }
.vto-privacy { text-align: center; margin: 10px 0 0; font: 400 11px/1.4 var(--vto-ff); color: #ccc; }

/* ── RESULT ── */
.vto-result-wrap { text-align: center; }
.vto-result__img-wrap { position: relative; margin: 0 auto 16px; max-width: 380px; border-radius: 20px; overflow: hidden; box-shadow: 0 16px 50px rgba(0,0,0,.12); animation: vto-resultReveal .8s cubic-bezier(.16,1,.3,1); }
.vto-result__img-wrap img { width: 100%; display: block; border-radius: 20px; }
.vto-result__shine { position: absolute; top: -100%; left: -100%; width: 200%; height: 200%; background: linear-gradient(135deg, transparent 40%, rgba(255,255,255,.3) 50%, transparent 60%); animation: vto-shineMove 2s ease .5s; pointer-events: none; }
@keyframes vto-shineMove { 0%{transform:translateX(-100%) translateY(-100%)} 100%{transform:translateX(100%) translateY(100%)} }
@keyframes vto-resultReveal { 0%{opacity:0;transform:scale(.85) translateY(20px)} 100%{opacity:1;transform:scale(1) translateY(0)} }
.vto-result__product-tag { display: inline-flex; align-items: center; gap: 8px; padding: 8px 16px 8px 8px; background: #f8f8f8; border-radius: 12px; margin-bottom: 16px; }
.vto-result__product-tag img { width: 32px; height: 32px; border-radius: 8px; object-fit: cover; }
.vto-result__product-tag span { font: 600 13px/1 var(--vto-ff); color: #333; }
.vto-result__btns { display: flex; gap: 10px; margin-bottom: 12px; }
.vto-disclaimer { font: 400 10px/1.4 var(--vto-ff); color: #bbb; margin: 0; padding: 0 16px; }
.vto-powered { text-align: center; padding: 12px 0 4px; font: 400 11px/1 var(--vto-ff); color: #bbb; letter-spacing: 0.3px; }
.vto-powered a { color: #888; text-decoration: none; font-weight: 600; }
.vto-powered a:hover { color: #555; }

/* ── LOADING ── */
.vto-loading-wrap { text-align: center; padding: 32px 0 16px; }
.vto-loader-circle { margin: 0 auto 20px; width: 72px; height: 72px; position: relative; }
.vto-spinner-arc { animation: vto-spin 1.2s linear infinite; transform-origin: center; }
@keyframes vto-spin { to { transform: rotate(360deg); } }
.vto-loading__title { font: 700 18px/1 var(--vto-ff); color: #111; margin: 0 0 6px; }
.vto-loading__sub { font: 400 13px/1.4 var(--vto-ff); color: #999; margin: 0 0 20px; }
.vto-progress { width: 100%; height: 4px; background: #f0f0f0; border-radius: 4px; overflow: hidden; margin-bottom: 20px; }
.vto-progress__bar { height: 100%; width: 0%; background: linear-gradient(90deg, var(--vto-accent), #a855f7); border-radius: 4px; animation: vto-progressGrow 28s ease forwards; }
@keyframes vto-progressGrow { 0%{width:0%} 30%{width:30%} 60%{width:55%} 85%{width:75%} 100%{width:92%} }
.vto-loading-steps { text-align: left; max-width: 240px; margin: 0 auto 16px; }
.vto-lstep { display: flex; align-items: center; gap: 10px; padding: 6px 0; font: 500 13px/1 var(--vto-ff); color: #ccc; transition: color .3s; }
.vto-lstep.active { color: #111; }
.vto-lstep.done { color: #22c55e; }
.vto-lstep__check { width: 18px; height: 18px; border-radius: 50%; border: 1.5px solid #e0e0e0; display: inline-flex; align-items: center; justify-content: center; font-size: 10px; transition: all .3s; flex-shrink: 0; }
.vto-lstep.active .vto-lstep__check { border-color: var(--vto-accent); background: var(--vto-accent); box-shadow: 0 0 0 3px rgba(124,58,237,.15); }
.vto-lstep.active .vto-lstep__check::after { content: ''; width: 4px; height: 4px; background: #fff; border-radius: 50%; }
.vto-lstep.done .vto-lstep__check { border-color: #22c55e; background: #22c55e; }
.vto-lstep.done .vto-lstep__check::after { content: '✓'; color: #fff; font-size: 10px; font-weight: 700; }
.vto-loading__time { font: 400 11px/1 var(--vto-ff); color: #ccc; }

/* ── ERROR ── */
.vto-error-wrap { text-align: center; padding: 40px 0 20px; }
.vto-error__emoji { font-size: 48px; margin-bottom: 12px; }
.vto-error__title { font: 700 20px/1 var(--vto-ff); color: #111; margin: 0 0 8px; }
.vto-error__msg { font: 400 14px/1.4 var(--vto-ff); color: #999; margin: 0 0 20px; }

/* ── ANIMATIONS ── */
@keyframes vto-fadeIn { from{opacity:0} to{opacity:1} }
@keyframes vto-scaleIn { from{opacity:0;transform:scale(.92) translateY(8px)} to{opacity:1;transform:scale(1) translateY(0)} }
@keyframes vto-slideUp { from{opacity:0;transform:translateY(10px)} to{opacity:1;transform:translateY(0)} }
@keyframes vto-pulse { 0%,100%{opacity:1} 50%{opacity:.5} }

/* ── MOBILE ── */
@media(max-width:480px){
  .vto-modal { padding: 24px 20px; border-radius: 24px; max-width: 100%; }
  .vto-title { font-size: 22px; }
  .vto-split__img-wrap { aspect-ratio: 2/3; }
  .vto-merge__img { width: 120px; height: 180px; }
  .vto-merge-canvas { height: 240px; }
}
</style>

<script>
(function(){
  const $=id=>document.getElementById(id);
  const el=$('vto');if(!el)return;
  const D=el.dataset;
  const btn=$('vto-btn'),popup=$('vto-popup'),overlay=$('vto-overlay');
  const fileInput=$('vto-file'),dzEmpty=$('vto-dz-empty'),dzPreview=$('vto-dz-preview'),prevImg=$('vto-preview-img');
  const submitBtn=$('vto-submit'),submitTxt=$('vto-submit-text');
  const resultImg=$('vto-result-img'),errorMsg=$('vto-error-msg');
  let file=null;

  function show(state){
    ['upload','merge','loading','result','error'].forEach(s=>{
      const el=$('vto-s-'+s);if(el)el.style.display=s===state?'block':'none';
    });
  }
  function openModal(){overlay.style.display='flex';show('upload');popup.style.display='none';}
  function closeModal(){overlay.style.display='none';reset();}
  function reset(){file=null;if(fileInput)fileInput.value='';if(dzEmpty)dzEmpty.style.display='block';if(dzPreview)dzPreview.style.display='none';submitBtn.disabled=true;submitTxt.textContent='Upload photo to try on';}
  function handleFile(f){
    if(!f||!f.type.startsWith('image/'))return;
    if(f.size>10*1024*1024){alert('File too large. Max 10 MB.');return;}
    file=f;
    const r=new FileReader();
    r.onload=e=>{prevImg.src=e.target.result;dzEmpty.style.display='none';dzPreview.style.display='block';submitBtn.disabled=false;submitTxt.textContent='Try it on →';};
    r.readAsDataURL(f);
  }
  btn.addEventListener('click',openModal);
  $('vto-popup-cta')?.addEventListener('click',openModal);
  $('vto-modal-close').addEventListener('click',closeModal);
  overlay.addEventListener('click',e=>{if(e.target===overlay)closeModal();});
  document.addEventListener('keydown',e=>{if(e.key==='Escape'&&overlay.style.display!=='none')closeModal();});
  $('vto-popup-close')?.addEventListener('click',e=>{e.stopPropagation();popup.style.display='none';sessionStorage.setItem('vto_dismissed','1');});
  if(!sessionStorage.getItem('vto_dismissed')){setTimeout(()=>{if(!overlay.style.display||overlay.style.display==='none')popup.style.display='block';},{{ block.settings.popup_delay }}000);}
  fileInput?.addEventListener('change',e=>handleFile(e.target.files[0]));
  $('vto-change-photo')?.addEventListener('click',e=>{e.stopPropagation();fileInput.click();});
  const dz=fileInput?.parentElement;
  if(dz){
    dz.addEventListener('dragover',e=>{e.preventDefault();dz.classList.add('dragover');});
    dz.addEventListener('dragleave',()=>dz.classList.remove('dragover'));
    dz.addEventListener('drop',e=>{e.preventDefault();dz.classList.remove('dragover');handleFile(e.dataTransfer.files[0]);});
  }

  // MERGE ANIMATION then submit
  submitBtn?.addEventListener('click',async()=>{
    if(!file)return;
    // Show merge animation first
    const mergeUser=$('vto-merge-user');
    if(mergeUser&&prevImg.src)mergeUser.src=prevImg.src;
    show('merge');
    const ml=$('vto-merge-product'),mr=$('vto-merge-user'),glow=$('vto-merge__glow');
    // Trigger merge after brief delay
    setTimeout(()=>{ml?.classList.add('merging');mr?.classList.add('merging');if(glow)glow.classList.add('active');},400);
    // Transition to loading after merge
    await new Promise(r=>setTimeout(r,2000));
    ml?.classList.remove('merging');mr?.classList.remove('merging');if(glow)glow.classList.remove('active');
    show('loading');startLoadingAnim();

    try{
      const fd=new FormData();
      fd.append('shop',D.shop);fd.append('product_id',D.pid);fd.append('product_title',D.ptitle);fd.append('product_image_url',D.pimg);fd.append('user_photo',file);fd.append('category',D.category||'dresses');
      const res=await fetch('/apps/tryon',{method:'POST',body:fd});
      const data=await res.json();
      if(res.ok&&data.success&&data.result_url){
        resultImg.src=data.result_url;show('result');return;
      }
      throw new Error(data.error||'AI unavailable');
    }catch(err){
      try{
        const userUrl=URL.createObjectURL(file);
        const canvas=document.createElement('canvas');
        canvas.width=600;canvas.height=800;
        const ctx=canvas.getContext('2d');
        const userImage=new Image();
        userImage.onload=()=>{
          ctx.drawImage(userImage,0,0,600,800);
          const prodImage=new Image();prodImage.crossOrigin='anonymous';
          prodImage.onload=()=>{
            const tmp=document.createElement('canvas');
            const tw=prodImage.naturalWidth,th=prodImage.naturalHeight;
            tmp.width=tw;tmp.height=th;const tc=tmp.getContext('2d');
            tc.drawImage(prodImage,0,0);
            const imgData=tc.getImageData(0,0,tw,th);const dd=imgData.data;
            for(let i=0;i<dd.length;i+=4){const br=(dd[i]+dd[i+1]+dd[i+2])/3;if(br>220&&Math.abs(dd[i]-dd[i+1])<25&&Math.abs(dd[i+1]-dd[i+2])<25)dd[i+3]=0;else if(br>200)dd[i+3]=Math.max(0,(255-br)*4);}
            tc.putImageData(imgData,0,0);
            const ratio=tw/th;let pw,ph,px,py;
            if(ratio>1.5){pw=300;ph=pw/ratio;px=(600-pw)/2;py=480;}
            else if(ratio>0.7){pw=240;ph=pw/ratio;px=(600-pw)/2;py=190;}
            else{pw=340;ph=pw/ratio;px=(600-pw)/2;py=200;}
            ctx.drawImage(tmp,px,py,pw,ph);
            ctx.fillStyle='rgba(124,58,237,0.9)';ctx.beginPath();ctx.roundRect(170,740,260,40,14);ctx.fill();
            ctx.fillStyle='#fff';ctx.font='600 14px Inter,sans-serif';ctx.textAlign='center';
            ctx.fillText('✨ Virtual Try-On by TryFit',300,766);
            resultImg.src=canvas.toDataURL('image/jpeg',0.92);show('result');
            // Log fallback try-on to server for dashboard tracking
            try{const lf=new FormData();lf.append('shop',D.shop);lf.append('product_id',D.pid);lf.append('product_title',D.ptitle);lf.append('log_only','1');fetch('/apps/tryon',{method:'POST',body:lf}).catch(()=>{});}catch(le){}
          };
          prodImage.onerror=()=>{errorMsg.textContent='Could not load product image';show('error');};
          prodImage.src=D.pimg;
        };
        userImage.src=userUrl;
      }catch(e2){errorMsg.textContent=e2.message||'Please try again.';show('error');}
    }
  });
  $('vto-retry')?.addEventListener('click',()=>{reset();show('upload');});
  $('vto-error-retry')?.addEventListener('click',()=>{reset();show('upload');});
  $('vto-addtocart')?.addEventListener('click',async()=>{
    const vid=document.querySelector('[name="id"]')?.value;
    try{if(vid){await fetch('/cart/add.js',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:vid,quantity:1})});}closeModal();window.location.href='/cart';}catch(e){window.location.href='/cart';}
  });

  let loadTimer=null;
  function startLoadingAnim(){
    const title=$('vto-loading-title'),sub=$('vto-loading-sub');
    const ls1=$('vto-ls1'),ls2=$('vto-ls2'),ls3=$('vto-ls3');
    const msgs=[
      {t:'Analyzing your photo...',s:'Detecting body shape & pose',step:1,ms:0},
      {t:'Understanding your body...',s:'Mapping proportions for perfect fit',step:1,ms:3000},
      {t:'Fitting the garment...',s:'Adjusting fabric drape & texture',step:2,ms:7000},
      {t:'Perfecting the details...',s:'Fine-tuning colors & shadows',step:2,ms:12000},
      {t:'Almost there...',s:'Adding final touches to your look',step:3,ms:18000},
      {t:'Putting it all together...',s:'Your personalized look is nearly ready',step:3,ms:24000},
    ];
    if(ls1){ls1.className='vto-lstep active';ls2.className='vto-lstep';ls3.className='vto-lstep';}
    msgs.forEach(m=>{
      setTimeout(()=>{
        if(title)title.textContent=m.t;
        if(sub)sub.textContent=m.s;
        if(m.step>=2&&ls1){ls1.className='vto-lstep done';ls2.className='vto-lstep active';}
        if(m.step>=3&&ls2){ls2.className='vto-lstep done';ls3.className='vto-lstep active';}
      },m.ms);
    });
  }
})();
</script>
{% endif %}

{% schema %}
{
  "name": "TryFit – Virtual Try-On",
  "target": "section",
  "templates": ["product"],
  "settings": [
    {
      "type": "header",
      "content": "Button"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Try On"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button Color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button Text Color",
      "default": "#FFFFFF"
    },
    {
      "type": "range",
      "id": "button_radius",
      "label": "Corner Radius",
      "min": 0,
      "max": 30,
      "step": 2,
      "default": 12,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Popup"
    },
    {
      "type": "text",
      "id": "popup_subtitle",
      "label": "Popup Subtitle",
      "default": "Upload a photo and try it instantly"
    },
    {
      "type": "range",
      "id": "popup_delay",
      "label": "Popup Delay",
      "min": 2,
      "max": 15,
      "step": 1,
      "default": 4,
      "unit": "s"
    },
    {
      "type": "header",
      "content": "Modal"
    },
    {
      "type": "text",
      "id": "modal_title",
      "label": "Modal Title",
      "default": "Try it before you buy"
    },
    {
      "type": "text",
      "id": "modal_subtitle",
      "label": "Modal Subtitle",
      "default": "Upload your photo and see this on you in seconds"
    },
    {
      "type": "header",
      "content": "AI Settings"
    },
    {
      "type": "select",
      "id": "garment_category",
      "label": "Garment Type",
      "default": "dresses",
      "info": "Upper body = tops/shirts. Lower body = pants/skirts. Full body = kurta sets, dresses, jumpsuits.",
      "options": [
        { "value": "upper_body", "label": "Upper Body (tops, shirts, jackets)" },
        { "value": "lower_body", "label": "Lower Body (pants, skirts)" },
        { "value": "dresses", "label": "Full Body (sets, dresses, jumpsuits)" }
      ]
    }
  ]
}
{% endschema %}
